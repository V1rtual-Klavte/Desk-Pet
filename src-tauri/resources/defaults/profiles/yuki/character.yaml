# 结城理的 Profile 角色元数据。
# 五层透明素材由 profile.yaml 的 parallax.layers 负责显示。
character:
  id: "yuki"
  name: "结城理"
  scale: 1.0
  scaleMode: smooth

# 保留一个兼容性的单帧入口；完整视觉由五层灵动图层合成。
animations:
  idle:
    loop: true
    frames:
      - { f: "materials/L2/body.png", d: 3000 }
