---
document_type: historical_implementation_plan
status: archived
current_reference: ../../current/system-design.md
---

# RUNTIME_DATA 变量池优化 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** LLM 输出末尾统一 `<RUNTIME_DATA>` 区块（情绪标签 + 变量更新），在 `generateReply()` 中一次性解析/写入/落盘，删除 When DSL 和全部 var 工具。

**Architecture:** 不改架构、不新建模块、只做减法。核心改动集中在 `generator.ts`（新增 `parseRuntimeData` + async `generateReply`）和 `variable-pool.ts`（新增 `batchWriteVars`），删除 `when-engine.ts` 和 `var.ts`。

**Tech Stack:** TypeScript, 现有 Tauri v2 + Vue 3

## Global Constraints

- 不考虑向后兼容，全部切成新的实现
- 不新建模块/文件，只改现有文件
- Card `# 行为进阶` DSL → 纯白话文本
- 删除 when-engine.ts (~220行) + var.ts (~110行)
- 净删 ~350 行代码

---

### Task 1: variable-pool.ts — 新增 batchWriteVars + 删除旧函数

**Files:**
- Modify: `src/services/personality/variable-pool.ts`

**Interfaces:**
- Produces: `batchWriteVars(updates: Record<string, string>): { written: string[], errors: string[] }`

- [ ] **Step 1: 添加 coerceValue + batchWriteVars**

在 `destroyPool()` (line 593) 之前插入：

```typescript
// ── 批量写入 (RUNTIME_DATA 解析后调用) ──

function coerceValue(raw: string, def: CardVariableDef): number | string | boolean | undefined {
  const trimmed = raw.trim()
  switch (def.type) {
    case "boolean": {
      if (trimmed === "true") return true
      if (trimmed === "false") return false
      return undefined
    }
    case "number": {
      const num = parseFloat(trimmed)
      if (isNaN(num)) return undefined
      if (def.min !== undefined && num < def.min) return undefined
      if (def.max !== undefined && num > def.max) return undefined
      return num
    }
    case "string": {
      const unquoted = ((trimmed.startsWith('"') && trimmed.endsWith('"')) || (trimmed.startsWith("'") && trimmed.endsWith("'")))
        ? trimmed.slice(1, -1)
        : trimmed
      if (def.enum && !def.enum.includes(unquoted)) return undefined
      return unquoted
    }
  }
}

export function batchWriteVars(updates: Record<string, string>): { written: string[]; errors: string[] } {
  const written: string[] = []
  const errors: string[] = []

  for (const [name, rawValue] of Object.entries(updates)) {
    const def = registry.find(d => d.scope === "card" && d.name === name)
    if (!def) { errors.push(`${name}: 未注册`); continue }
    if (def.updateBy !== "llm") { errors.push(`${name}: 不可写`); continue }

    const value = coerceValue(rawValue.trim(), def)
    if (value === undefined) { errors.push(`${name}: 类型/范围不符`); continue }

    pool.card[name] = { value, type: def.type, updatedAt: Date.now(), updatedBy: "llm" }
    savePending = true
    written.push(name)
  }

  if (written.length > 0) log.info("batchWrite:", written.join(", "))
  if (errors.length > 0) log.warn("batchWrite errors:", errors.join("; "))
  return { written, errors }
}
```

- [ ] **Step 2: 删除旧函数 + 旧 handler + 旧类型**

删除以下内容：
- `VarDef` 接口 (line 15-22) 和 `VarType` 类型 (line 13)
- `varRead()` (line 314-330)
- `varList()` (line 332-349)
- `varWrite()` (line 351-385)
- `varDelete()` (line 387-410)
- `inferType()` (line 603-607)
- `inferAndValidate()` (line 609-634)
- `buildVarReadHandler()` (line 638-646)
- `buildVarListHandler()` (line 648-655)
- `buildVarWriteHandler()` (line 657-667)
- `buildVarDeleteHandler()` (line 669-679)

- [ ] **Step 3: TypeScript 检查**

```bash
npx vue-tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 4: Commit**

```bash
git add src/services/personality/variable-pool.ts
git commit -m "refactor(variable-pool): 新增 batchWriteVars，删除 var_read/write/list/delete 及 handler 构造器"
```

---

### Task 2: types.ts — whenRules → whenText

**Files:**
- Modify: `src/services/personality/types.ts`

**Interfaces:**
- Produces: `CardSections.whenText: string`

- [ ] **Step 1: 改 types.ts**

```diff
- import type { WhenRule } from "./when-engine"

  // CardSections 中:
-   whenRules: WhenRule[]
+   whenText: string
```

- [ ] **Step 2: TypeScript 检查**

```bash
npx vue-tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 3: Commit**

```bash
git add src/services/personality/types.ts
git commit -m "refactor(types): whenRules: WhenRule[] → whenText: string"
```

---

### Task 3: loader.ts — When 区块改为纯文本存储

**Files:**
- Modify: `src/services/personality/loader.ts`

**Interfaces:**
- Consumes: `CardSections.whenText` (from Task 2)

- [ ] **Step 1: 改 parseSections() 中 When 部分 (line 228-239)**

```diff
-   // 行为进阶规则
-   const whenRaw = sections["行为进阶"] || ""
-   const whenRules: import("./when-engine").WhenRule[] = []
-   const blocks = whenRaw.split(/^##\s*规则:\s*/m).filter(b => b.trim())
-   for (const block of blocks) {
-     const nm = block.match(/^(.+)$/m)
-     const wm = block.match(/^when:\s*(.+)$/m)
-     const tm = block.match(/^语气:\s*([\s\S]+?)(?=\n\w|$)/m)
-     if (nm && wm) {
-       whenRules.push({ name: nm[1]!.trim(), when: wm[1]!.trim(), tone: (tm?.[1] ?? "").trim() })
-     }
-   }
+   // 行为进阶 — 原文作为 LLM 语气指引
+   const whenText = (sections["行为进阶"] || "").trim()
```

```diff
  return {
    ...
-   emotionRaw, emotionMappings, whenRules, mustRules,
+   emotionRaw, emotionMappings, whenText, mustRules,
    variableDefs,
  }
```

- [ ] **Step 2: TypeScript 检查**

```bash
npx vue-tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 3: Commit**

```bash
git add src/services/personality/loader.ts
git commit -m "refactor(loader): # 行为进阶 区块存原文到 whenText"
```

---

### Task 4: emotion.ts — 删 stripEmotionTag + 改 formatEmotionForPrompt

**Files:**
- Modify: `src/services/personality/emotion.ts`

- [ ] **Step 1: 删除 stripEmotionTag + EMO_TAG_RE (line 19-28)**

```diff
- const EMO_TAG_RE = /^\[emo:(\w+)\]\s*/
- 
- export function stripEmotionTag(raw: string): { text: string; emotionKey: string | null } {
-   const match = raw.match(EMO_TAG_RE)
-   if (!match) return { text: raw, emotionKey: null }
-   return { text: raw.slice(match[0].length), emotionKey: match[1] }
- }
```

- [ ] **Step 2: 改 formatEmotionForPrompt (line 89-101)**

```typescript
export function formatEmotionForPrompt(mappings: EmotionMapping[]): string {
  if (mappings.length === 0) return ""

  const tags = mappings.map(m =>
    `${m.key}(${m.expression}${m.sound ? `,${m.sound}` : ""})`
  ).join(", ")

  return `[回复元数据]
你的回复末尾必须附加一个 RUNTIME_DATA 区块，系统自动剥离，用户不可见。

格式：
<RUNTIME_DATA>
emotion: <情绪标签>
<变量名>: <值>
</RUNTIME_DATA>

- emotion 必填。可用标签: ${tags}
- 其他行选填，只在 Card 变量有变化时写入（变量名: 新值）`
}
```

- [ ] **Step 3: TypeScript 检查**

```bash
npx vue-tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 4: Commit**

```bash
git add src/services/personality/emotion.ts
git commit -m "refactor(emotion): 删 stripEmotionTag，formatEmotionForPrompt → RUNTIME_DATA 指令"
```

---

### Task 5: generator.ts — parseRuntimeData + async generateReply

**Files:**
- Modify: `src/services/reply/generator.ts`

**Interfaces:**
- Consumes: `resolveEmotion` (emotion.ts), `batchWriteVars` + `savePoolToDisk` (variable-pool.ts, Task 1)
- Produces: `generateReply(raw, card?, options?) → Promise<ReplyResult>`

- [ ] **Step 1: 更新 imports**

```typescript
import { createLogger } from "@/services/logger"
import { resolveEmotion } from "@/services/personality/emotion"
import { batchWriteVars, savePoolToDisk } from "@/services/personality/variable-pool"
import type { PersonalityCard } from "@/services/personality/types"
```

- [ ] **Step 2: 添加 parseRuntimeData (generateReply 之前)**

```typescript
const RUNTIME_RE = /<RUNTIME_DATA>\s*([\s\S]*?)\s*<\/RUNTIME_DATA>/i

interface ParsedRuntime {
  text: string
  runtime: { emotion: string | null; vars: Record<string, string> }
}

function parseRuntimeData(raw: string): ParsedRuntime {
  const match = raw.match(RUNTIME_RE)
  if (!match) return { text: raw, runtime: { emotion: null, vars: {} } }

  const block = match[1]
  const beforeBlock = raw.slice(0, match.index)
  const afterBlock = raw.slice(match.index! + match[0].length)
  const text = (beforeBlock + afterBlock).trim()

  let emotion: string | null = null
  const vars: Record<string, string> = {}

  for (const line of block.split("\n")) {
    const trimmed = line.trim()
    if (!trimmed) continue
    const colonIdx = trimmed.indexOf(":")
    if (colonIdx === -1) continue
    const key = trimmed.slice(0, colonIdx).trim()
    const value = trimmed.slice(colonIdx + 1).trim()
    if (!key || !value) continue
    if (key === "emotion") { emotion = value }
    else { vars[key] = value }
  }

  return { text, runtime: { emotion, vars } }
}
```

- [ ] **Step 3: 改 generateReply 为 async**

```typescript
export async function generateReply(
  raw: string,
  card?: PersonalityCard | null,
  options: ReplyOptions = {},
): Promise<ReplyResult> {
  const { maxLength = DEFAULT_MAX_LENGTH } = options
  const emotionMappings = card?.sections.emotionMappings ?? []

  // 1. 解析 RUNTIME_DATA
  const { text: cleanText, runtime } = parseRuntimeData(raw)

  // 2. 情绪解析
  const { expression, sound } = resolveEmotion(runtime.emotion, emotionMappings)

  // 3. 变量批量写入
  if (runtime.vars && Object.keys(runtime.vars).length > 0) {
    batchWriteVars(runtime.vars)
  }

  // 4. 落盘
  await savePoolToDisk()

  // 5. trim + 截断
  let text = cleanText.trim()
  if (text.length > maxLength) {
    const truncated = text.substring(0, maxLength)
    const lastPeriod = Math.max(
      truncated.lastIndexOf("。"),
      truncated.lastIndexOf("！"),
      truncated.lastIndexOf("？"),
      truncated.lastIndexOf("\n"),
    )
    text = lastPeriod > maxLength * 0.5
      ? truncated.substring(0, lastPeriod + 1) + "…"
      : truncated + "…"
  }

  return { text, emotionKey: runtime.emotion, expression, sound }
}
```

- [ ] **Step 4: TypeScript 检查**

```bash
npx vue-tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 5: Commit**

```bash
git add src/services/reply/generator.ts
git commit -m "refactor(generator): generateReply 改为 async，新增 parseRuntimeData"
```

---

### Task 6: builder.ts — 删除 VAR_TOOLS + When DSL + 变量操作规则

**Files:**
- Modify: `src/services/context/builder.ts`

- [ ] **Step 1: 更新 imports**

```diff
- import { evaluateWhenEngine } from "@/services/personality/when-engine"
```

保留 `formatPoolForPrompt`, `formatAllRules`, `formatEmotionForPrompt` imports。

- [ ] **Step 2: 改 When 语气区域 (line 64-68)**

```typescript
  // ── ③ 语气指引 ──
  if (s?.whenText) {
    systemPrompt += `\n\n[语气指引]\n${s.whenText}`
  }
```

- [ ] **Step 3: 删 VAR_TOOLS + 简化工具声明 (line 76-104)**

```typescript
  // ── ⑤ 工具声明 ──
  const tools = decideTools(isActiveMessage ?? false)
```

删 `hasVarWrite` 和 `hasGeneralTools` 变量。

- [ ] **Step 4: 删变量操作规则 (line 83-104)**

删除整个 `if (hasVarWrite) { ... }` 块。

- [ ] **Step 5: 简化工具提示 (line 115-125)**

```typescript
  if (tools.length > 0) {
    systemPrompt += "\n\n你可以使用工具完成任务。需要工具时只输出工具调用。完成后基于结果简短回复。"
  } else {
    systemPrompt += "\n\n请简短口语化回复。"
  }
```

- [ ] **Step 6: 删 VAR_TOOLS 常量 + 改 decideTools (line 138-149)**

```typescript
function decideTools(isActive: boolean): ToolDeclaration[] {
  if (isActive) return []
  return getToolDeclarations()
}
```

- [ ] **Step 7: TypeScript 检查**

```bash
npx vue-tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 8: Commit**

```bash
git add src/services/context/builder.ts
git commit -m "refactor(builder): 删 VAR_TOOLS+变量操作规则+When DSL，精简工具提示"
```

---

### Task 7: agent-loop.ts — await generateReply + 删 savePoolToDisk

**Files:**
- Modify: `src/services/engine/agent-loop.ts`

- [ ] **Step 1: 删除 savePoolToDisk import**

```diff
- import { refreshVariablePool, getPoolSnapshot, savePoolToDisk, updateInteractionVar, applyResetPolicies, getSessionStart } from "@/services/personality/variable-pool"
+ import { refreshVariablePool, getPoolSnapshot, updateInteractionVar, applyResetPolicies, getSessionStart } from "@/services/personality/variable-pool"
```

- [ ] **Step 2: generateReply 加 await + 删 savePoolToDisk 调用**

```diff
-   const processed = generateReply(rawReply, card)
+   const processed = await generateReply(rawReply, card)

    ...
-   await savePoolToDisk()
```

- [ ] **Step 3: TypeScript 检查**

```bash
npx vue-tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 4: Commit**

```bash
git add src/services/engine/agent-loop.ts
git commit -m "refactor(agent-loop): await generateReply，删除 savePoolToDisk 调用"
```

---

### Task 8: personality/index.ts — 移除旧导出

**Files:**
- Modify: `src/services/personality/index.ts`

- [ ] **Step 1: 删除 when-engine 导出 (line 27-28)**

```diff
- export { evaluateWhenEngine, evaluateWhen } from "./when-engine"
- export type { WhenRule } from "./when-engine"
```

- [ ] **Step 2: 更新 variable-pool 导出 (line 24)**

```diff
- export { initVariablePool, refreshVariablePool, getPoolSnapshot, formatPoolForPrompt, varRead, varWrite, varList, varDelete, saveVariablePoolAsync, savePoolToDisk, savePoolToDiskStrict, loadCardVars, readSystemVars, updateInteractionVar, setSessionVars, applyResetPolicies, computeSystemVariables, destroyPool, setSessionStart, getSessionStart, getVariableRegistry } from "./variable-pool"
- export type { VariablePool, VarDef, VariablePoolRuntimeState } from "./variable-pool"
+ export { initVariablePool, refreshVariablePool, getPoolSnapshot, formatPoolForPrompt, batchWriteVars, saveVariablePoolAsync, savePoolToDisk, savePoolToDiskStrict, loadCardVars, readSystemVars, updateInteractionVar, setSessionVars, applyResetPolicies, computeSystemVariables, destroyPool, setSessionStart, getSessionStart, getVariableRegistry } from "./variable-pool"
+ export type { VariablePool, VariablePoolRuntimeState } from "./variable-pool"
```

- [ ] **Step 3: 更新 emotion 导出 (line 33)**

```diff
- export { stripEmotionTag, parseEmotionMappings, resolveEmotion, formatEmotionForPrompt } from "./emotion"
+ export { parseEmotionMappings, resolveEmotion, formatEmotionForPrompt } from "./emotion"
```

- [ ] **Step 4: TypeScript 检查**

```bash
npx vue-tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 5: Commit**

```bash
git add src/services/personality/index.ts
git commit -m "refactor(personality): 移除 when-engine/var/stripEmotionTag 导出"
```

---

### Task 9: 删除文件 + init.ts

**Files:**
- Delete: `src/services/personality/when-engine.ts`
- Delete: `src/services/tool/local/var.ts`
- Modify: `src/services/init.ts`

- [ ] **Step 1: 删除文件**

```bash
rm src/services/personality/when-engine.ts
rm src/services/tool/local/var.ts
```

- [ ] **Step 2: 改 init.ts**

```diff
- import { registerVarTools } from "@/services/tool/local/var"

-   registerVarTools()  // 变量池工具（var_read/write/list/delete）— 所有模式可用
```

- [ ] **Step 3: TypeScript 检查**

```bash
npx vue-tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 4: Commit**

```bash
git add src/services/personality/when-engine.ts src/services/tool/local/var.ts src/services/init.ts
git commit -m "refactor: 删除 when-engine.ts + var.ts，更新 init.ts"
```

---

### Task 10: AITab.vue + Card 模板

**Files:**
- Modify: `src/components/settings/AITab.vue`
- Modify: `src/services/personality/cards/_template.md`
- Modify: `src/services/personality/cards/pchan.md`

- [ ] **Step 1: AITab.vue line 536**

```diff
-             <span>{{ card.sections.whenRules.length }} 条行为规则</span>
+             <span v-if="card.sections.whenText">有语气指引</span>
```

- [ ] **Step 2: _template.md — 更新注释说明**

`# 情绪表达` 注释中 `[emo:key]` → `<RUNTIME_DATA>` 格式说明。
`# 行为进阶` 注释中 DSL 语法文档 → 白话描述指引。

- [ ] **Step 3: pchan.md — 行为进阶改白话**

```diff
- ## 规则: 想睡觉
- when: hour >= 2 AND hour <= 8
- 语气: 困得要命，说话断断续续...
- 
- ## 规则: 默认
- when: true
- 语气: 慵懒随性，半躺半坐的感觉。嘴硬心软
+ - 凌晨2点到早上8点：困得要命，说话断断续续，抱怨为什么这么早/这么晚还在
+ - 用户连续忽略你3次以上：极度懒散，半睡半醒，爱回不回
+ - 用户忽略你1-2次：催促，嘴上抱怨
+ - 默认：慵懒随性，半躺半坐的感觉。嘴硬心软
```

- [ ] **Step 4: 检查其他 Card**

```bash
grep -l "when:" src/services/personality/cards/*.md
```

如有 DSL 格式，同样改为白话。

- [ ] **Step 5: Commit**

```bash
git add src/components/settings/AITab.vue src/services/personality/cards/
git commit -m "refactor: 更新 AITab+Card模板，行为进阶→白话，情绪表达→RUNTIME_DATA"
```

---

### Task 11: Live Test 更新

**Files:**
- Delete: `src/services/__tests__/live/contracts/when-engine.contract.ts`
- Delete: `src/services/__tests__/live/scenes/when-engine/when联动.scene.ts`
- Modify: `src/services/__tests__/live/contracts/variable-pool.contract.ts`
- Modify: `src/services/__tests__/live/contracts/emotion.contract.ts`
- Modify: `src/services/__tests__/live/scenes/variable-pool/亲密度提升.scene.ts`
- Modify: `src/services/__tests__/live/scenes/variable-pool/越界拒绝.scene.ts`
- Modify: `src/services/__tests__/live/index.live.test.ts`

- [ ] **Step 1: 删除 When 测试文件**

```bash
rm src/services/__tests__/live/contracts/when-engine.contract.ts
rm -rf src/services/__tests__/live/scenes/when-engine/
```

- [ ] **Step 2: 更新 index.live.test.ts**

删除 when-engine 模块注册和 var tool 相关引用。

- [ ] **Step 3: 更新 variable-pool contract + scenes**

将 var_write tool call 场景 → batchWriteVars / RUNTIME_DATA 解析场景。

- [ ] **Step 4: 更新 emotion contract**

`[emo:key]` 前缀剥离 → RUNTIME_DATA emotion 提取。

- [ ] **Step 5: Commit**

```bash
git add src/services/__tests__/live/
git commit -m "test: 更新 live test 适配 RUNTIME_DATA+batchWriteVars，删 when-engine 测试"
```

---

### Task 12: 全量验证

- [ ] **Step 1: TypeScript 全量检查**

```bash
npx vue-tsc --noEmit 2>&1
```

预期：零新增 error。

- [ ] **Step 2: Rust 检查**

```bash
cd src-tauri && cargo check 2>&1
```

预期：零 error。

- [ ] **Step 3: 最终 Commit**

```bash
git add -A
git commit -m "refactor: RUNTIME_DATA 统一输出区域，删除 when-engine+var tools，净删~350行"
```
