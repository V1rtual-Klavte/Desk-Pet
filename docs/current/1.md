队列这块，应该用pi提供的队列。但是不要显式地展现出来，按照我的规范，做成可配置的，可以开关显示队列状态，默认不显示队列状态，像聊天一样的，连续发消息，只是后台有个队列，一条条处理，不是像coding agent一样显示队列状态。
对不同模型的支持，应该全部切换成pi ai，删除现在的provider代码，以及兼容层，确保干净，同时确保其他位置同步新改动
对于我自己写的工具，全面替换成pi agent core提供的，这块好好讨论
ui这块应该好好讨论下，工具调用流式没问题，回复的流式应该是可配置，默认关，然后桌宠ui也讨论下。我认为开发阶段可以都用上，然后最后我做取舍
上下文压缩这块应该讨论下，是和下一阶段memory一起做还是怎么做
