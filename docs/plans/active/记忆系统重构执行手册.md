---
document_type: execution_runbook
status: harness_h1_h4_verified_memory_p6_pending
updated_at: 2026-09-18
historical_baseline: ../../history/implementation/记忆系统重构执行手册-2026-09-18基线.md
---

# 记忆系统重构执行手册

## 当前检查点

未完成工作已统一迁入 [2026-09-20 收尾清单](2026-09-20收尾清单.md)；本页不再维护待办列表，只保留验证证据、环境注意与未验证边界。

- **已完成并验证**：AgentHarness 迁移 H-1–H-4 全部落地——JSONL 会话底座（JsonlSessionRepo）、HarnessSlot 运行内核（`lane.prompt/steer/followUp/abort`、持久 inbox、逐请求 usage、流式正文）、Harness 压缩调度与 `before_compaction` 陪伴摘要，并删除 RuntimeQueue/AgentSlot/旧压缩调度及全部过渡层。迁移协议、替换映射与批次设计见 [AgentHarness 迁移方案基线](../../history/implementation/AgentHarness迁移方案-2026-09-18基线.md)；行为契约见[运行时契约](../../current/runtime-contract.md)与[当前记忆](../../current/memory.md)。
- **同轮落地的口径**：上下文窗口默认 128k、最低 64k，低于下限在设置页保存与模型解析处报错；压缩设置经 `toHarnessEstimateTokens()` 换算到上游估算口径，阈值早于宿主硬预算触发。
- **实现与修复过程**：回合死锁（`captureSnapshot` 在 hook 内 await 同一 lane 的写命令）、宿主 inbox 镜像与 lane 持久 inbox 分叉、RUNTIME_DATA 结算读取被剥离后的正文、压缩检查点的空切点、followUp 场景前提等，逐条记录与证据见[归档基线](../../history/implementation/记忆系统重构执行手册-2026-09-18基线.md)。
- **2026-09-20 收尾轮（本轮）**：§2.1 三条断链、§2.2 四个配置字段、PI-1（输入意图显式选择 / 排队视图 / 单项撤回，Slash 执行收敛到 ingress 并按 `busyPolicy` 准入）、PI-2（`defineTool` 唯一构造入口、策略字段合并、Rust 应用级许可所有者、纯读并行与写互斥）、PI-4（usage 按 purpose 单列）、§3 硬预算超限改走 Harness 溢出恢复与上下文估算按字符分类、§4.1 L5/L8/L10/L11、§4.2 U5 会话读模型均已落地，整轮严格门禁见下表。**剩余未做项与待决策项见[收尾清单](2026-09-20收尾清单.md)**——其中 L3 闭包门禁与「写类工具是否在工具侧声明 `ask`」两条是开放问题，须先决策才能实施。
- **本轮的口径订正**：方案 §5.1 原有「按逐工具 `executionMode` 调度并行」的描述与上游不符——Harness 只读 run 级 `toolExecution`，不再读逐工具字段，此前 `harness-slot` 写死 `"sequential"`，故「纯读并行」从未生效；改为由 Rust 许可所有者保证互斥后 `toolExecution` 才置 `"parallel"`。方案内已加实施订正注记。
- **环境注意**：macOS 锁屏时 WebKit 会把窗口判为遮挡并挂起页面 JS（实测每个 scene 约 6 秒后整体停摆，进程存活但无进展）。跑 Live 门禁前必须解锁屏幕并保持点亮；`always_on_top`/`caffeinate` 无效。

## 已有验证证据

2026-09-20 收尾轮的整轮严格门禁是最近一次完整验证；同日早前一轮（表情 / 动画 / 音效移除之后）与更早轮次保存在[2026-09-18 归档基线](../../history/implementation/记忆系统重构执行手册-2026-09-18基线.md)，不代表当前源码结论。

| 项 | 记录 |
|---|---|
| 时间 | `2026-09-20T06:21:26.281Z` |
| 数据集 / schema | `2026-09-20.9` / `desk-pet-live/v2` |
| runId | `e837edda-0c0a-4168-8b32-027544363c5c` |
| 选项 | `strictContracts: true`、`repeat: 3` |
| 结果 | 91 cases / 273 trials，0 failed / skipped / timeout，`pass@3 = pass^3 = 1`，耗时 51.5s |
| Contract | 8 个 Contract 的 sourceHash 有效，missing / gaps / datasetErrors 均为空 |
| 报告 | `/Users/v1rtual/.deskpet-live-test-reports/2026-09-20T06-21-26-281Z.txt` |
| 报告记录的 HEAD | `6ed1d47c0f50ab82be3c436a5df881a4d3675c02`；测试时工作区包含尚未提交的收尾改动，不能把该提交单独当作完整受测源码 |
| 未验证范围 | Windows CI、跨进程重启的人工冒烟、长期记忆质量评测；**本轮新增的 UI 未经人工查看**（ChatPanel 排队面板与投递意图、设置页「对话投递」与「只读并行」控件、`DebugBar` 的 Σ 用量面板），Live Test 宿主不渲染 UI；环境为 macOS Tauri WebView，不能由总通过数推断每项均覆盖工具或 IPC |

## 验收边界

P6 完成前不得宣称自动长期记忆已接通。SQLite 迁移不改变会话正文真相源；摘要、工具输出和主动消息不自动升级为用户事实。当前运行时规则见[当前运行时契约](../../current/runtime-contract.md)，目标协议见[记忆系统运行时契约](./记忆系统运行时契约.md)，原阶段记录见[2026-09-17 基线](../../history/implementation/记忆系统重构执行手册-2026-09-17基线.md)与[2026-09-18 基线](../../history/implementation/记忆系统重构执行手册-2026-09-18基线.md)。
