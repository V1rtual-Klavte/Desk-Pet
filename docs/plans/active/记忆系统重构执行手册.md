---
document_type: execution_runbook
status: active
created_at: 2026-09-14
updated_at: 2026-09-15
source_plan: 记忆系统运行时契约.md
scope: runtime-foundation-memory-integration
---

# 记忆系统重构执行手册

这是一份可以从任意新会话继续执行的工作手册。设计原则和协议细节以[记忆系统运行时契约](./记忆系统运行时契约.md)为准；本手册只回答“下一步改什么、怎么验证、何时算完成”。

## 0. 新会话启动方式

每次接力先执行下面的固定动作：

```bash
cd /Users/v1rtual/WEB/someworks/Desk-Pet
git status --short
cat docs/plans/active/记忆系统重构执行手册.md
cat docs/plans/active/记忆系统运行时契约.md
```

然后读取本文件的“当前检查点”，只处理 `next_action` 指定的工作。必须先向用户说明思路、影响文件和验证方式，得到同意后才开始编码；本次创建执行手册的同意不等于后续代码修改的同意。

执行过程中遵守以下规则：

- 先读后写，先用 `rg` 找全消费者；不凭文档或旧测试结果推断当前代码已经实现。
- 不清理、覆盖或提交用户已有的未提交改动；不主动修改真实配置、`.gitignore`或运行时数据。
- 每个阶段都按“源码分析 → 最小实现 → Contract/Scene 更新 → 类型/编译检查 → Live Test → 文档检查点”推进。
- 代码或契约改变后，旧的 `sourceHash` 和旧测试结论作废；必须重新生成或重新分析受影响 Contract。
- 所有主动消息、hook、恢复消息、子代理消息都必须带来源和污染级别，不能伪装成用户输入或长期记忆来源。

## 1. 当前状态（2026-09-15）

- 运行时契约已从 `proposed` 更新为 `partially_implemented`；逐节落地情况见其 §1.1。
- P0–P3 已完成代码门禁并有 Live Test 证据；P4、P5 只部分落地，遗留项见下表状态列。
- 已落地的运行时基础：`engine/runtime/` 协议类型、事件兼容适配、快照脱敏、只读 trace、RuntimeQueue、SessionTurnStore、PlanCheckpointStore、按 sessionId 隔离的 AgentSlot、ContextKernel 六层预算、双阶段 PromptSnapshot、工具审计与稳定错误码。
- 仍未落地：`app_open` 路径校验、工具输出 spill、API round 分级压缩、网络 redirect/私网 IP 防护、Memory Eval。自研 HookBus 已删除（`cd85856`，生产消费者为零），Rust bash 最终基线已按两层 token 策略落地（`eb9a312`）。
- 长期记忆自动提取、Prompt 自动召回、画像安全写入和 dreaming 都不能作为已完成能力对外宣称 —— 空 `MemoryProvider` 保持长期召回关闭。
- 当前目标是 P6：MemoryKernel、画像写入和 dreaming。

## 2. 阶段总览与门禁

| 阶段 | 目标 | 主要落点 | 阶段通过条件 | 状态 |
|---|---|---|---|---|
| P0 | 类型、事件、快照、trace 和 fake provider 基础 | `src/services/engine/`、`src/services/agent/memory/`、`src/services/__tests__/live/` | 兼容旧会话；快照可审计；fake provider 场景稳定 | ✅ 已完成 |
| P1 | Ingress、SessionTurnStore、来源和原子持久化 | `agent/runner.ts`、`engine/preprocessor.ts`、memory/session store、必要的 Rust 专用命令 | queued → done 可恢复；重复 request 不重复落盘 | ✅ 已完成 |
| P2 | AgentSlot、队列、steer/follow-up、Plan 恢复 | `engine/pi/runtime.ts`、`engine/planner.ts`、session 事件 | 忙碌时输入有回执；旧 generation 不结束新 run | ✅ 已完成 |
| P3 | ContextKernel、PromptSnapshot、画像投影、空 MemoryProvider | `context/builder.ts`、新 runtime/context 模块 | block 顺序、预算、来源和最终 payload 可验证 | ✅ 已完成 |
| P4 | tool pair 安全压缩、spill、版本化摘要 | `engine/compactor.ts`、`tool/router.ts`、session files | 不切断 tool pair；摘要失败保留原 round | ⚠️ 部分：生效的是阈值触发的 LLM 结构化摘要（不切片，天然不拆 pair）；`compactMessages`/`groupMessageUnits` 无生产调用点，spill 与版本化摘要未实施 |
| P5 | HookBus、安全 deny-first、工具审计、网络/沙箱门禁 | `safety/`、`tool/`、Pi hooks、Rust commands | 安全拒绝先于执行；超时/取消/错误可审计 | ⚠️ 部分：见 §6 遗留项 |
| P6 | MemoryKernel、长期事实、画像写入、dreaming 和评测 | `agent/memory/`、eval/contracts/scenes | 仅可信来源可 deep write；通过多轮稳定性门禁 | ⬜ 未开始 |

阶段不得跳过。P1 依赖 P0 的事件和 hash，P2 依赖 P1 的 turn 状态，P3 依赖稳定 transcript，P4/P5 依赖 PromptSnapshot 和来源元数据，P6 依赖全部前置审计面。

## 3. P0 交付摘要（已完成）

P0 建立了不改变用户可见行为的运行时基础协议和观测面，工作包如下：

| 工作包 | 交付 |
|---|---|
| P0-01 运行时基础类型 | `engine/runtime/types.ts`：`MessageOrigin`、`MessageMeta`、`IngressEnvelope`、`SessionEvent`、`SessionTurnRecord`、`PromptSnapshot`、`QueueEntry`、`QueueAck`、`PlanRecord`、`PlanStepRecord`，全部带 `schemaVersion`、稳定 ID、时间戳和幂等键 |
| P0-02 会话事件兼容协议 | `agent/memory/events.ts`：Markdown 预览 + HTML comment 完整 JSON；旧记录填充 `schemaVersion=0` 兼容默认值；单条损坏不清空整段会话 |
| P0-03 PromptSnapshot 与脱敏 hash | `engine/runtime/snapshot.ts`：system blocks、tool schema/policy hash、消息 hash、transform 链、估算 token、cache 与 redaction 列表；原始 payload 不落盘 |
| P0-04 只读 Pi 观测钩子 | `pi/runtime.ts` 接入 `subscribe` / `onPayload` / `onResponse` / `transformContext`，只记录不改变语义 |
| P0-05 Fake Provider 与最小场景 | `__tests__/live/fake-provider.ts` + `memory-prompt-snapshot`、`memory-old-session-compat`、`memory-active-origin`、`memory-tool-pair-baseline` |
| P0-06 验证 | `pnpm run test:types`、`pnpm test -- --module memory/agent-runtime --strict` 均通过；命令与报告保留在 git 历史 |

回溯细节见 git 历史；P0 阶段不保留逐条执行步骤在本手册中。

## 4. P1–P6 执行清单

### P1：Ingress 与 SessionTurnStore

- 将 `preprocessor` 的模块级 `lastUserText/lastUserTime` 降为请求级字段；输入保留 raw/normalized text、querySource、priority、taint。
- 先持久化 `queued`，再更新 UI 和调用 Pi；实现单会话写锁、事件幂等、版本/CAS、临时文件 + rename 原子提交。
- 启动时扫描 `dispatching/running/waiting_tool`，转为 `interrupted` 或 `unknown_side_effect` 并写 recovery event。
- active 入口显式写 `origin=active`、`querySource=active_monitor`、`eligibleForMemory=false`。

门禁：重启后 queued turn 可恢复；重复 request 只有一份事实事件；通用 `file_write` 语义不被悄悄改变，如需原子写则新增专用 session 命令并注册到 `lib.rs`。

### P2：AgentSlot、队列与 Plan

- 按 `sessionId` 管理驻留 Agent，移除全局忙碌锁作为事实来源。
- 实现 `persisted → reserved → dispatched → accepted` 回执和 `now > next > later` 优先级；steer/follow-up 必须先落盘再调用 Pi。
- 每个异步 drain 带 generation；旧 generation 不能结束新 run。
- Plan/step、子 agent、工具开始/结束写 checkpoint；外部副作用无完成凭证时标记 `unknown_side_effect`，不自动重试。

门禁：忙碌时输入不丢失且有 queued/steered/followup/deferred/failed 回执；恢复后不重复执行未知副作用。

### P3：ContextKernel 与 PromptSnapshot

- 将 `buildPrompt()` 内部改为固定层级 block：`static → dynamic → profile → memory → transcript → ephemeral`，保留兼容入口。
- User.md/画像只作为 projection；长期记忆先接空 `MemoryProvider`，不宣称已自动召回。
- 在 `transformContext` 后及最终 Provider payload 处各捕获一次快照，最终 payload 以 `onPayload` 为准。
- prompt rewrite 只产生派生字段，保存 input/output hash、reason、derivedFrom，不覆盖用户原文。

门禁：预算不足有明确裁剪原因；active/hook/recovery 只进入 ephemeral 或 UI-only；快照可按 request/turn/run 完整追踪。

### P4：压缩与工具输出 spill

- L0 清理 UI-only/重复块并将超长 tool output spill 到受限文件；L1 按完整 API round 摘要；L2 只删除最旧完整 round。
- tool call/result 必须成组；摘要失败保留原 round，不伪造孤立 `role=tool` 摘要。
- compaction event 带 coveredEventIds、input/output hash、generation 和版本；旧任务不能覆盖新 snapshot。

门禁：压缩前后 tool pair、最近用户/助手、Plan checkpoint 和安全上下文完整；spill 文件路径经过 Rust 校验且不泄露敏感内容。

### P5：Hook、安全、工具和错误

- Hook 门禁：工具前后置回调由 Pi 原生 `beforeToolCall` / `afterToolCall` 承担（fail-closed）。自研 HookBus 已因生产消费者为零删除；若要重做外层 hook，必须先有真实消费者，并沿用 hookId、deadline、错误分类、重入保护和 taint 继承的约定。
- 安全顺序固定为 deny-first：来源/作用域 → 静态规则 → 动态策略 → 用户确认 → 工具执行；不能先命中 session trust 就放行。
- ToolRouter 记录 opId、schema/policy hash、超时、AbortSignal、结果截断和 spill；网络工具统一走 allowlist、DNS/IP 防护、请求超时和响应上限。
- 取消、窗口关闭、安全拒绝、超时和 shutdown 都写结构化 turn/tool 状态；持久化错误文本使用脱敏摘要。

门禁：禁止调用在执行前可观察；任何 hook/工具异常都有稳定 error code；Rust 命令返回 `AppResult<T>` 并完成 `invoke_handler!` 注册。

### P6：MemoryKernel、画像和 dreaming

- 召回与提取分离；提取只读取 `eligibleForMemory=true` 且 `taint=trusted_user` 的 user 事件。
- 以 `MemoryCandidate`/`ProfileMemoryCandidate` 表示候选，保存来源事件、策略 hash、置信度、状态和冲突关系；模型不能直接覆盖 User.md/MEMORY.md。
- dreaming 分 Light/REM/Deep，具备 lease、dreamId、输入/输出版本、幂等键、preimage 和失败重试。
- deep write 前必须通过敏感字段、冲突、过期、来源和用户可追溯性门禁；active、hook、recovery、system、subagent 默认不得写入长期事实。

门禁：长期事实写入可回滚、可解释、可按来源删除；多轮真实 trial 的 `pass@k`/`pass^k` 达到发布要求，未通过时保持候选态。

## 5. 最小回归用例注册表

下表是重构的最低覆盖集合与实际落地的对照。缺口项必须先补 Scene/Contract，再实现对应代码。

| 目标 case | 实际 caseId | 状态 |
|---|---|---|
| `memory-queue-before-dispatch` | `queued-before-dispatch` | ✅ 已落地（命名未带 `memory-` 前缀） |
| `memory-steer-during-tool` | `memory-steer-during-tool` | ✅ 已落地 |
| `memory-followup-after-turn` | `memory-followup-after-turn` | ✅ 已落地 |
| `memory-active-origin` | `memory-active-origin` | ✅ 已落地 |
| `memory-plan-resume` | `memory-plan-resume` | ✅ 已落地 |
| `memory-prompt-snapshot` | `memory-prompt-snapshot` | ✅ 已落地 |
| `memory-profile-rewrite` | `memory-profile-rewrite` | ✅ 已落地 |
| `memory-context-budget` | `memory-context-budget` | ✅ 已落地 |
| `memory-old-session-compat` | `memory-old-session-compat` | ✅ 已落地 |
| `memory-safety-deny-first` | — | ❌ 缺失（部分由 `safety-trust-lifecycle` 覆盖） |
| `memory-compaction-tool-pair` | — | ❌ 缺失（P4 遗留） |
| `memory-tool-spill` | — | ❌ 缺失（P4/P5 遗留） |
| `memory-dreaming-gate` | — | ❌ 缺失（P6） |

每个 case 必须说明入口（`production` 或测试专用）、来源/污染、持久化断言、错误/边界标签和重复 trial 要求。跨模块改动运行完整 `pnpm test`；发布前运行：

```bash
pnpm test -- --strict --repeat 3 --report json
```

## 6. 当前检查点（接力唯一依据）

新会话只修改这一节，不要把聊天摘要当作状态来源。`status` 只有四个取值：`not_started`、`in_progress`、`blocked`、`verified`；标 `verified` 必须同时有本阶段的 `tests` 和 `evidence`。

```yaml
phase: P5
task: P5-07
status: in_progress
last_verified_at: 2026-09-15T16:39:45+08:00
changed_files:
  # 仅列本轮 P5-07；P0–P5 早期改动见 git 历史与各 Contract 的 sourceHash
  - src/services/safety/checker.ts
  - src/services/safety/index.ts
  - src/services/tool/types.ts
  - src/services/tool/router.ts
  - src/services/tool/local/pi-tools.ts
  - src/services/tool/local-extra/app.ts
  - src/services/tool/pi/tauri-execution-env.ts
  - src/services/engine/pi/runtime.ts
  - src-tauri/src/commands/tool_exec.rs
  - src-tauri/src/commands/personality_fs_cmd.rs
  - src-tauri/src/paths.rs
  - src-tauri/Cargo.toml
  - src/services/__tests__/live/contracts/safety.contract.ts
  - src/services/__tests__/live/contracts/tool-execution.contract.ts
  - src/services/__tests__/live/scenes/safety/安全等级边界.scene.ts
  - src/services/__tests__/live/scenes/tool-execution/工具取消.scene.ts
  # 注：src/services/agent/provider.ts 已随 6ee2aa6 删除，不再列入
tests:
  - pnpm run test:types
  - cargo test --manifest-path src-tauri/Cargo.toml
  - pnpm test -- --module tool-execution --strict
  - pnpm test -- --module safety --strict --repeat 2
evidence:
  - command: pnpm run test:types
    result: pass
    at: 2026-09-15T16:39:45+08:00
    report: "vue-tsc --noEmit + cargo check 均通过"
  - command: cargo test --manifest-path src-tauri/Cargo.toml
    result: pass
    at: 2026-09-15T16:39:45+08:00
    report: "32 passed；含本批新增的 tool_exec（尾部窗口截断与旧实现逐字段比对）与 paths（符号链接叶子）用例"
  - command: pnpm test -- --module tool-execution --strict
    result: pass
    at: 2026-09-15T16:39:45+08:00
    report: "5/5，零 timeout 零 skip"
  - command: pnpm test -- --module safety --strict --repeat 2
    result: unstable
    at: 2026-09-15T16:39:45+08:00
    report: "改动后 7 次运行中 5 次 25/25 全绿，2 次各 1 个场景超时（137.1s / 139.7s，均为单个场景耗尽 120s 预算）。已做改动前基线对照：基线 380ab45 连跑 5 次全部 25/25、耗时 14.8–24.6s。Fisher 精确检验 p≈0.47，样本不足以判定显著；改动中也没有能让 Provider 网络调用停滞的机制。归因未定，故不标 verified"
remaining:
  - "safety Live Test 存在间歇性场景超时，根因是纯逻辑场景仍走真实 LLM 回合（计划 §3.5 的 L7）与超时后在飞回合渗进下一场景（L10）"
  - "网络 redirect 与私网 IP 防护未实施：src/services/engine/pi/net-guard.ts 只校验协议、超时和响应体上限"
  - "缺失场景：memory-safety-deny-first、memory-tool-spill"
  - "prompt(string) 续跑路径待核：pi/runtime.ts 在末条不是 user 时仍调用 agent.prompt()"
  - "只读并行是死代码：agent 级 toolExecution: \"sequential\" 使 per-tool executionMode 失效（pi-agent-core agent-loop.js 判序）"
next_action: 本轮安全止血（计划 §3.1 的 S1–S6）已全部落地；P5 收口前先做 safety 超时的基线对照，或直接推进计划 §3.5 的 L2（entry 为 unit 的场景不再走模型）
approval_needed: no
notes: P5 已交付 deny-first 信任顺序、工具审计（opId/policyHash/outcome）、取消与超时稳定错误码、Provider 协议与响应体上限、Rust bash 两层 token 最终基线（eb9a312）。本轮补上 P5-07 的 app_open 路径校验，并顺带完成计划 §3.1 的 S1–S6：bash_exec 异步化与尾部窗口读取、工具输出 spill、符号链接叶子拒绝、文件路径分级接到文件工具、会话信任收窄为「工具 + 本次参数」。HookBus 已随 cd85856 删除，工具门禁由 Pi 原生 beforeToolCall/afterToolCall 承担。P4 已交付阈值触发的 LLM 结构化摘要（不切片），分级压缩与版本化摘要仍属缺口。长期召回、画像写入和 dreaming 未接通；P0–P5 早期的 evidence 保留在 git 历史。
```

建议的 `evidence` 写法：

```yaml
- command: pnpm run test:types
  result: pass
  at: 2026-09-14T00:00:00+08:00
  report: null
```

## 7. 新会话接力提示词

复制下面这段作为新会话第一条消息：

```text
继续 Desk-Pet 的记忆系统重构。请先读取 AGENTS.md、docs/plans/active/记忆系统重构执行手册.md、docs/plans/active/记忆系统运行时契约.md，以及执行手册“当前检查点”。

只处理当前检查点的 next_action，不扩大范围。先检查代码和现有 Live Contract，给出：1）事实与缺口；2）最小改动文件；3）影响的 Contract/Scene；4）验证命令；5）风险和回滚点。未经我同意不要编码、不要提交。

实现后更新执行手册的 phase/task/status、changed_files、tests、evidence、next_action；代码契约变化必须刷新 sourceHash。长期记忆自动提取、Prompt 自动召回、画像写入和 dreaming 在各自门禁通过前均视为未接通。
```

## 8. 完成定义与明确不做

整个重构只有同时满足以下条件才算完成：会话、队列、Plan、PromptSnapshot、压缩、工具、安全、画像和长期记忆均有持久化状态；重启可恢复且未知副作用不自动重试；来源/污染可追踪；关键 Contract 严格通过并在三次 trial 中稳定；当前文档与代码一致。

在此之前明确不做：向量数据库或云端记忆迁移、重写 Profile 渲染、把所有 Pi Harness 内部类直接引入顶层、用 UI 状态代替 session 真相源、把 dreaming 做成每轮前台隐式副作用。
