---
document_type: execution_runbook
status: harness_h1_h4_verified_one_open_case_memory_p6_pending
updated_at: 2026-09-18
historical_baseline: ../../history/implementation/记忆系统重构执行手册-2026-09-17基线.md
---

# 记忆系统重构执行手册

## 当前检查点

- **原前置交付基线**：`c2127bd`；原 P0–P5 的事件/队列、AgentSlot、Context、压缩 checkpoint、权限、Provider 边界、Skill 元数据和轻量生命周期已交付。此记录不代表后续新需求或发现的边界均已覆盖。
- **本次 Pi 核对基线**：`a7fd393`、pi-agent-core/pi-ai 0.85.1；本轮仅落盘方案及订正当前说明，没有实现或重新运行代码门禁。
- **新增待办**：steer/followup 显式双模式、逐条消费/请求/回复关联、收尾竞态、统一工具策略与只读并行、原生 hook、逐请求 usage/流式正文、压缩校准。目标与验收统一见[Pi 运行时与工具协议方案](Pi运行时与工具协议建设方案.md)。
- **H-1–H-4 实现记录（2026-09-17，基线 `40d926b` + 未提交工作树）**：H-1 已提交（`aba73b9`：存储底座 + 一致性套件，harness-storage 3 场景与 tool-execution 9/9 已真机 IPC 跑通）。H-2–H-4 未提交：内核换 AgentHarness/HarnessSlot（lane.prompt/steer/followUp/abort、持久 inbox、逐请求 usage、流式正文、中断恢复状态）；会话正文迁 `sessions/` JSONL；压缩走 Harness 调度 + `before_compaction` 陪伴摘要（settings 由 contextBudget 推导）；删除 RuntimeQueue/AgentSlot/旧压缩调度/旧队列恢复（净删 3000+ 行）。已执行门禁：`pnpm run test:types` 通过（主会话复跑 exit 0）；Live 契约/场景已按 SKILL 静态随迁（agent-runtime/memory 重写、契约 sourceHash 校验、dataset 终值 `2026-09-17.4`）。**未运行任何 Live Test**；未验证：全量/严格门禁、fake/真实 Provider 场景、跨进程重启、Windows。
- **兼容层清理（2026-09-17，同工作树）**：按用户指令移除全部过渡层并记入 AGENTS「开发阶段不保留兼容层」——会话目录直接用 `sessions/`（index.json 共存已实测）、`file_list` Rust 全字段（TS 回填桥删除 + cargo 单测）、旧 Markdown 会话格式与解析整层删除（session-files/events/session-turn-store 与死导出）、Plan checkpoint 改接 `deskpet.plan_checkpoint` 条目、Rust 死命令（list_session_files/session_file_write_atomic/delete_session_file）删除。验证：`test:types` 通过、`cargo test` 34/34；未运行 Live Test。
- **集中验证（2026-09-18，基线 `a671254` + 未提交修复）**：首次真机跑通完整门禁，本轮结果只在此处记录一次。
  - 范围：`pnpm run test:types`、`test:release`（严格 Contract + 3 trials）。macOS Tauri WebView + fake Provider；未跑 Windows、未做手工冒烟。
  - 结果（最终一轮，`c86b7b6` 之后的工作树）：`test:types` 通过；`test:release` 严格 Contract 9/9 `valid`、`missing`/`gaps`/`datasetErrors` 为空；88 cases / 264 trials，**261 通过 / 3 失败 / 0 跳过 / 0 超时**，`pass@3 = pass^3 = 98.86%`；总判定为失败（唯一失败用例见下）。中间一轮为 252/264、4 个用例失败，修复后收敛至此。
  - **修复的致命缺陷（本次迁移引入）**：`captureSnapshot` 在 Harness hook / 事件处理器内 `await` 对同一 lane 的写入命令，与 drive 持有的命令锁循环等待 —— 每个 production 回合都会在 usage 事件后永久卡死（`lane.prompt` 不返回、`abortAgentRuns` 不生效、scene 超时无法推进）。改为宿主只入队（`queueAuditEntry`），drive 结束后在 `execute` finally 里 flush。
  - **修复的宿主记账缺陷**：`collectPendingDelivery` 无条件清空宿主 inbox 镜像，使 `snapshot().queued` 与 lane 持久 inbox 分叉（`runtime-nextrun-inbox` 失败）。镜像改为只由 `queue_update` 事件更新。
  - **修复的测试缺陷**：`harness-storage/执行环境文件树` 新增断言用 `createTempDir()` 未规范化返回值拼字符串比对，macOS temp 在 `/var`→`/private/var` 符号链接下必然失败；改为与 canonical root 比对。同一批按审查结论修掉 4 条高危（`tools.mcp.enabled` 读写值与派生值拆开、`waitForIdle`/`dispose`/`abort`/`discardInterrupted` 不再吞错或伪装成功、SessionTabs 历史加载错误态）。
  - **同轮修复的结算缺陷**：`afterResponse` 在提交前剥离 RUNTIME_DATA 并返回新消息，事件里的最终助手消息因此已无标签；结算却直接解析该文本，导致 emotion 映射与 RUNTIME_DATA 变量写入整条链路失效（`emotion-runtime-effect`、`variable-affection-praise` 共 4 个用例失败）。改为在剥离前记录「原始正文 ↔ 剥离后正文」配对，结算按配对取回原文。
  - **仍未通过（1 用例 × 3 trials）**：`memory-compaction-checkpoint`，T3 `没有产生 compaction 条目: declined`。已定位到 `before_compaction` 拿到的 `preparation` 里 `messagesToSummarize` 与 `turnPrefixMessages` 均为空：实测 `keepRecentTokens=3648`，`tokensBefore` 在载荷 repeat 224 / 476 两种规模下分别为 8210 / 9050，都是空切点。载荷规模不是唯一变量，`findCutPoint` 给出空范围的真实条件仍未定位，按现状保留原载荷，**不视为已通过**。
  - **同轮修复的场景前提**：`memory-followup-after-turn` 原先在工具执行期投递却期待 followUp——旧内核用 `agentSlots.markDeliveryPhase` 强行置位 settling，该入口已随迁移删除，settling 窗口只存在于 turn_end 与 run_end 之间、场景无法稳定命中。改为用显式 kind 走同一条 `lane.followUp` 通道验证通道语义，并断言工具期投递模式必须是 steer；契约 ar-03 的描述同步改为不声称验证 settling 自动路由。
  - **环境注意**：macOS 锁屏时 WebKit 会把窗口判为遮挡并挂起页面 JS（实测每个 scene 约 6 秒后整体停摆，任务管理器无 CPU、进程存活）。跑 Live 门禁前必须解锁屏幕并保持点亮；`always_on_top`/`caffeinate` 无效。
- **已知断链与剩余待办**：`nextRun` 未消费输入暂无 UI 展示；`/compact` 忙时有排队消息只保守拒绝；宿主本地硬预算超限以报错代替 Harness 溢出恢复；usage purpose 单列（PI-4）、插话双模式 UI（PI-1）、工具策略/只读并行（PI-2）未实施；开发数据根残留孤立的旧 `pi-sessions/` 目录（旧数据可弃，未清理）。
- **下一步**：定位 `findCutPoint` 给出空可摘要范围的真实条件（需要 instrument 上游 compaction 内部，而非只调场景载荷），据此决定是修压缩设置推导还是场景设计；随后 Windows CI、PI-1 / PI-2 / P6。

## 已有验证证据

下列是 2026-09-17 的历史集中门禁记录，**不是本轮重新运行的结论**；源码或 Contract 改动后必须重新分析 sourceHash 并重新验证。

| 项 | 记录 |
|---|---|
| 时间 | `2026-09-17T03:27:13.694Z` |
| 数据集 / schema | `2026-09-17.1` / `desk-pet-live/v2` |
| runId | `6eeb7091-8312-485c-95e6-ba3a093102b7` |
| 结果 | 89 cases / 267 trials，0 failed / skipped / timeout，`pass@3=1`、`pass^3=1` |
| Contract | 8 个 Contract 的 sourceHash 有效，missing / gaps / datasetErrors 均为空 |
| 报告 | `/Users/v1rtual/.deskpet-live-test-reports/2026-09-17T03-28-16-041Z.txt` |
| 报告记录的 HEAD | `726dc0e6473b82abd3e48960883c842b995daabd`；测试时包含尚未提交的前置改动，不能把该提交单独当作完整受测源码 |
| 未验证范围 | Windows CI、长期记忆质量评测；环境为 macOS Tauri WebView，unit/fake/真实 Provider 场景各按声明入口验证，不能由总通过数推断每项均覆盖工具或 IPC |

## P6 最小实施顺序

1. 定义 SQLite schema、迁移与导入/导出，并保留 Markdown 的人工可读投影边界。
2. 建立候选记录、来源/版本/置信度、冲突与撤销模型；默认不自动写入。
3. 实现按 scope 和 token 预算受控的 recall，再把只读 projection 接入 Runtime。
4. 设计画像写入确认、纠正/遗忘与 dreaming 的后台调度。
5. 为提取、召回、污染隔离、压缩恢复、冲突和撤销补 Contract/Scene；最后集中执行类型、模块和严格 Live 门禁。

## 验收边界

P6 完成前不得宣称自动长期记忆已接通。SQLite 迁移不改变会话正文真相源；摘要、工具输出和主动消息不自动升级为用户事实。当前运行时规则见[当前运行时契约](../../current/runtime-contract.md)，目标协议见[记忆系统运行时契约](./记忆系统运行时契约.md)，原阶段记录见[历史基线](../../history/implementation/记忆系统重构执行手册-2026-09-17基线.md)。
