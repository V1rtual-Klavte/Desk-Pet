---
document_type: backlog
status: active
updated_at: 2026-09-20
supersedes: ../../history/analysis/收尾清单-2026-09-20基线.md
---

# 未完成工作与已知缺口

本页是 `plans/active/` 的**唯一未完成工作总表**，也是集中验证证据与未验证边界的记录点；active 不再为单一主题另开文档。已完成方案的正文与当时证据在 `docs/history/`，入口见本页末节。本页只维护"还没做的事"和"已知但未修的问题"，不复制已完成成果的叙述。

条目状态只有三种：**进行中**（已定边界，可实施）、**待实施**（边界未定或依赖前置）、**开放问题**（需先决策）。

---

## 1. Pi 协议剩余批次

PI-1（输入意图显式选择 / 排队视图 / 单项撤回，Slash 收敛到 ingress 并按 `busyPolicy` 准入）与 PI-2 核心（ToolDef 统一策略、`defineTool` 唯一构造入口、Rust 应用级许可所有者、纯读并行与写互斥）已于 2026-09-20 落地并通过严格门禁；协议正文见 [Pi 方案 2026-09-20 基线](../../history/implementation/Pi运行时与工具协议建设方案-2026-09-20基线.md)。剩余：

| 项 | 状态 | 内容 | 完成条件 |
|---|---|---|---|
| PI-1 余项 | 待实施 | 更细的逐项证据状态（`request_prepared` 等）与停止入口 | 尚无边界定义，实施前先补方案章节 |
| PI-2 余项 | 待实施 | 薄 `BaseTool`（当前零消费者）、设置页展示完整 policy | 薄层需要有真实消费者；设置页须能显示策略而非只显示名字 |

**已决策、不再实施**（论证见 [Pi 方案基线](../../history/implementation/Pi运行时与工具协议建设方案-2026-09-20基线.md) §4.3 与[收尾清单基线](../../history/analysis/收尾清单-2026-09-20基线.md)）：

- 写类工具（`write` / `edit`）**保持** `policy.permission: passthrough`，不改为 `ask`——工具侧 `ask` 会压过 base `allow`，等于用静态声明静默改掉 `safety.mode = just_do_it` 的语义；要收紧应改安全模式或 checker。
- L3 依赖闭包门禁**不加**：闭包差 89/92 是 barrel 噪声，收益不抵 hash 维护成本。
- 许可额度的窗口销毁残留**不修**：不存在能让它阻塞到谁的路径，为零影响缺口引入无法验证的事件接线违背「类型检查不能代替运行验证」。

## 2. 平台与发布

| 项 | 状态 | 当前边界 | 下一步 |
|---|---|---|---|
| P8 设置窗口置顶 | 待实施（仅剩实机） | Windows 的 `#[cfg(target_os = "windows")]` 分支已补（`SetWindowPos` + `SetForegroundWindow`），离线类型实验证明被编译，`9e55aba` 之后的 Windows CI 原生 check 已通过 | 在 Windows 机器上复核实际 z 序与前台效果 |
| main 窗口销毁后无法重建 | 待实施 | 托盘「显示」与左键单击在 `get_webview_window("main")` 为 `None` 时都不重建、不报错。**成因修正**：上游 tauri-runtime-wry 在最后一个窗口销毁时会触发进程退出（本仓未拦 `ExitRequested`），并不存在"只剩托盘、用户只能退出"的僵尸态；只有 settings/layer-editor 尚存时才会出现托盘独存（此为上游源码推演，实机复核时一并确认） | 托盘「显示」重建 main；必须同时拦 `ExitRequested`（与挂在 `RunEvent::Exit` 的 MCP 子进程回收耦合），并在实机验证 |
| 发布 | 待实施 | macOS 签名、公证尚未交付 | 按发布目标单独安排 |
| Windows CI | 进行中 | 双平台 job 自 2026-09-15 起持续在跑（`6d339cf` 轮 Windows 原生 `cargo check` 已 success）；未收口的是 job 改名 `verify` 后新增 `cargo test --lib` 的 Windows 首跑——`b7a50f6` 轮仍在运行 | 等该轮 CI 结论；失败则修 Windows 专属分支 |

## 3. 已知代码缺口（2026-09-20 review 发现，均未修）

| # | 位置 | 问题 | 影响边界 |
|---|---|---|---|
| 1 | [compactor.ts](../../../src/services/engine/compactor.ts) 的摘要输入投影 | 不查 `resultProjection`，对所有工具结果统一缩短 | `preserve` 工具的长结果在**压缩素材**里仍被 L0 缩短；会话存档全文不受影响，与 `types.ts` 中 preserve 的表述有出入 |
| 2 | 全部 10 个生产工具 | 均声明 `historyCompaction: "summarize"` | `retain` 保护机制在线但无生产声明者，当前只由测试驱动 |
| 3 | [tool_permit.rs](../../../src-tauri/src/commands/tool_permit.rs) 的 `tool_permit_release` | 只按 requestId 删租约，不校验调用方身份 | 任何窗口拿到（可猜或可复用的）requestId 就能释放他人的在飞独占额度；跨窗口完整性目前依赖 requestId 构成 |
| 4 | [App.vue](../../../src/App.vue) 的 Windows 提层调用 | 300ms `setTimeout` 后 invoke，窗口未建好时静默空转；settings 无二次调用（layer-editor 在 AppearanceTab 有） | 置顶分支可能不生效，且无日志无返回值，实机复核时需一并覆盖 |
| 5 | [scene-runner.ts](../../../src/services/__tests__/live/scene-runner.ts) 的 `classifyError` | 仍是无边界 `/5\d\d/`（`classifyTurnFailure` 已于 `ab5e195` 收紧） | 同类数字误分类风险；只影响 Live 报告分类，不影响产品行为 |

## 4. 验证缺口

以下均已实现，缺的是验证手段；完成前不得宣称已发布就绪：

- **Windows 运行时行为**：本机 macOS 只能证明 cfg 隔离与类型正确，不证明运行时行为；P8、托盘与窗口生命周期均无 Windows 实机记录。
- **跨进程重启的人工冒烟**：`app_restart` 的进程级重启链路未在真实应用里点过。
- **UI 未经人工查看**：ChatPanel 排队面板与投递意图选择、设置页新增控件（对话投递 / 只读并行）、`DebugBar` 的 Σ 用量面板、`effectMode` 门控后的光标跟随；Live Test 宿主不渲染 UI。设置页「修改 → 保存 → 文件回读 → 关闭重开」的往返未执行。
- **长期记忆质量评测**：P6 未实施，不适用。

## 5. 长期记忆 P6（下一阶段）

目标契约见[记忆系统运行时契约](记忆系统运行时契约.md)。P6 完成前不得宣称自动长期记忆已接通。

1. 定义 SQLite schema、迁移与导入/导出，保留 Markdown 的人工可读投影边界。
2. 建立候选记录、来源与版本、置信度、冲突与撤销模型；默认不自动写入。
3. 实现按 scope 和 token 预算受控的 recall，再把只读 projection 接入 Runtime。
4. 设计画像写入确认、纠正与遗忘、dreaming 的后台调度。
5. 为提取、召回、污染隔离、压缩恢复、冲突与撤销补 Contract / Scene，最后集中执行类型、模块与严格 Live 门禁。

## 6. 当前验证证据

| 项 | 记录 |
|---|---|
| 时间 | `2026-09-20T08:24:55.773Z`（同日 `08:23:14.386Z` 另有一轮，结论相同） |
| 数据集 / schema | `2026-09-20.12` / `desk-pet-live/v2` |
| 选项 | `strictContracts: true`、`repeat: 3` |
| 结果 | 91 cases / 273 trials，0 failed / skipped / timeout，`pass@3 = pass^3 = 1` |
| Contract | 8 个 Contract 的 sourceHash 有效，missing / gaps / datasetErrors 均为空 |
| 报告 | `~/.deskpet-live-test-reports/2026-09-20T08-24-55-773Z.txt` 与 `...08-23-14-386Z.txt` |
| 记录的 HEAD | `b7a50f6`，工作区干净 |
| 历史证据 | H-1–H-4 及更早轮次见[执行手册 2026-09-20 基线](../../history/implementation/记忆系统重构执行手册-2026-09-20基线.md) |

**环境注意**：macOS 锁屏时 WebKit 会把窗口判为遮挡并挂起页面 JS（实测每个 scene 约 6 秒后整体停摆，进程存活但无进展）。跑 Live 门禁前必须解锁屏幕并保持点亮；`always_on_top`/`caffeinate` 无效。

## 7. 归档入口

- [收尾清单 2026-09-20 基线](../../history/analysis/收尾清单-2026-09-20基线.md)：表情/动画/音效移除收尾、L3 与许可残留的决策论证、当时的验证缺口快照。
- [Pi 方案 2026-09-20 基线](../../history/implementation/Pi运行时与工具协议建设方案-2026-09-20基线.md)：PI-1/PI-2 协议正文、实施订正与验收设计。
- [执行手册 2026-09-20 基线](../../history/implementation/记忆系统重构执行手册-2026-09-20基线.md)：H-1–H-4 实现细节、06:58 轮证据与当时的未验证边界。
