// ==========================================
// 记忆系统 — 本地去重与显式维护边界
// ==========================================

// ── 会话结束 ──

export function onSessionEnd(): void {
  // 会话结束不隐式发起 LLM 整理或事实提取；长期记忆写入闭环在 P6 实施前不存在自动链路。
}
